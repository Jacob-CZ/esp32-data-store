generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model GpsData {
  id         Int      @id @default(autoincrement())
  latitude   Float
  longitude  Float
  recordedAt DateTime
  createdAt  DateTime @default(now())
}
